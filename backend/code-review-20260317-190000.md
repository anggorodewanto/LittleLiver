---BEGIN CODE REVIEW---

## Summary

Phase 16 adds caloric calculation logic layered onto the existing feeding CRUD: formula-based computation with `cal_density`, breast milk defaulting to 20 kcal/oz, breast-direct using `default_cal_per_feed`, `used_default_cal` tracking, validation for `cal_density` with breast-direct (no volume), and a recalculate endpoint on `PUT /api/babies/:id?recalculate_calories=true`. The implementation is well-structured, spec-compliant, and thoroughly tested.

All tests pass. Coverage is 98.5% (model), 87.3% (store), 83.5% (handler). The touched code paths for Phase 16 are well covered.

**Phase 16 TDD checklist verification:**
1. Formula feeding with cal_density calculates calories correctly -- TESTED (unit + integration + handler)
2. breast_milk defaults to 20 kcal/oz -- TESTED (unit + integration + handler)
3. breast-direct uses default_cal_per_feed -- TESTED (unit + integration + handler)
4. used_default_cal flag set correctly -- TESTED (table-driven unit test + integration)
5. breast-direct with cal_density returns 400 -- TESTED (unit + handler)
6. Recalculate endpoint updates all affected entries -- TESTED (store + handler)
7. Normal PUT returns baby object, PUT with recalculate_calories=true returns envelope -- TESTED (handler)

**Proof of progress:** All calorie computation scenarios tested; recalculate endpoint working.

**Verdict: Implementation is correct and complete. No blocking issues found.**

### [LOW] Handler package coverage below 90% target

- **File:** `backend/internal/handler/` (package-level)
- **Line(s):** N/A - structural issue
- **Perspective:** QA
- **Finding:** The handler package coverage is 83.5%, below the project's 90% target specified in CLAUDE.md.
- **Reasoning:** While the Phase 16 code paths themselves appear well-tested, the overall package coverage sits below the threshold. This is likely due to pre-existing untested paths in other handlers (e.g., OAuth callback, session management) rather than gaps in the Phase 16 implementation specifically.
- **Consequence:** Accumulating coverage debt below the 90% threshold could mask regressions in under-tested handler code paths.
- **Suggestion:** This is not a Phase 16 issue per se, but worth noting. Consider adding tests for uncovered handler paths in a subsequent pass to bring the package above 90%.

### [LOW] Blank line at line 48 in feedings handler

- **File:** `backend/internal/handler/feedings.go`
- **Line(s):** L48
- **Perspective:** Tech Lead
- **Finding:** There is an extra blank line between the `validate()` method and the `feedingResponse` struct definition.
- **Reasoning:** While not a functional issue, gofmt-clean code typically avoids double blank lines between declarations.
- **Consequence:** Minor readability inconsistency.
- **Suggestion:** Remove the extra blank line between the `validate()` closing brace and the `feedingResponse` comment.

---END CODE REVIEW---
