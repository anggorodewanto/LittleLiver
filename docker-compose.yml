services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_PATH=/data/littleliver.db
      - STATIC_DIR=/static
    volumes:
      - littleliver-data:/data

volumes:
  littleliver-data:
